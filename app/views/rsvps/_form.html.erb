<%= form_for(@rsvp, :html => {:class => "form-inline"}) do |f| %>
  <% if @rsvp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rsvp.errors.count, "error") %> prohibited this rsvp from being saved:</h2>

      <ul>
      <% @rsvp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :guest_id %>
    <%= f.number_field :guest_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %>
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.check_box :answer %>
  </div>
  <div class="actions">
    <%= f.submit :type => "submit", :class => "btn btn-primary" %>
  </div>
<% end %>
